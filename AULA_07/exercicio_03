#include<stdio.h>

typedef struct{
    int x,y;
}ponto;

int main(){

    ponto p1, p2, p;

    scanf("%d %d", &p1.x, &p1.y);
    scanf("%d %d", &p2.x, &p2.y);

    p.x = p1.x + p2.x;
    p.y = p1.y + p2.y;

    printf("%d %d\n",p.x,p.y);

   return 0;
}
