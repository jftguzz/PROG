#include<stdio.h>

int main(){
    char ch;
    int count = 0;
    do{
        scanf("%c",&ch);
        if(ch=='a' || ch=='A')
            count++;
    }while(ch!=' ');
    printf("%d",count);
    return 0;
}
