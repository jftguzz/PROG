#include<stdio.h>


int main(){
    int numero=-1, ate, resto;

    while (numero<=0)
        scanf("%d", &numero);

    for (ate = 1; ate <= numero; ate ++){
        resto=numero%ate;
        if (resto==0){
            printf("%d ", ate);
        }
    }
    return 0;
}

/*
#include <stdio.h>

int main()
{
    int n, i;
    do{
        scanf("%d",&n);
    }while(n<=0);

    for(i=1; i <= n; i++){
        if (n%i == 0){
            printf("%d ",i);
        }
    }
    return 0;
}
